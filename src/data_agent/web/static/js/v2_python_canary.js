(() => {
    const state={filename:'',sessionId:'',turnId:'',blocks:[],supplementalArtifacts:[],running:false};
    const byId=(id)=>document.getElementById(id);const escapeHtml=(v)=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
    const setStatus=(text)=>{byId('status').textContent=text;};function showError(message=''){byId('error').textContent=message;byId('error').classList.toggle('visible',Boolean(message));}
    function updateUrl(){if(!state.sessionId||!state.turnId)return;const url=new URL(window.location.href);url.searchParams.set('session_id',state.sessionId);url.searchParams.set('turn_id',state.turnId);history.replaceState({},'',url);byId('run-id').textContent=`session ${state.sessionId} · turn ${state.turnId}`;}
    function renderBlocks(blocks){state.blocks=blocks||[];byId('answer').innerHTML=state.blocks.map((block)=>{const limits=(block.limitations||[]).length?`<ul>${block.limitations.map((item)=>`<li>${escapeHtml(item)}</li>`).join('')}</ul>`:'';return `<article class="answer-block" data-calibration="${escapeHtml(block.calibration)}" data-block-type="${escapeHtml(block.block_type)}"><h2>${escapeHtml(block.headline)}</h2><p>${escapeHtml(block.narrative)}</p>${limits}</article>`;}).join('');}
    function progress(label){byId('progress').classList.add('visible');const item=document.createElement('div');item.className='progress-item';item.innerHTML=`<span class="dot"></span><span>${escapeHtml(label)}</span>`;byId('progress-list').appendChild(item);}
    async function uploadSelected(){const file=byId('file').files[0];if(!file)throw new Error('请先选择数据文件。');const form=new FormData();form.append('file',file);setStatus('正在上传');const response=await fetch('/api/upload',{method:'POST',body:form});const body=await response.json();if(!response.ok)throw new Error(body.error||'上传失败');state.filename=body.filename;byId('file-state').textContent=`已上传：${body.filename}`;}
    function handleEvent(event,data){if(event==='turn_started'){progress('分析会话已建立');state.sessionId=data.session_id;state.turnId=data.turn_id;updateUrl();}else if(event==='commitment_snapshot')progress('核心描述承诺已冻结');else if(event==='tool_started')progress(data.name==='exploratory_python'?'正在隔离执行探索代码':'正在执行结构化描述统计');else if(event==='tool_finished')progress(data.name==='exploratory_python'?`探索执行结束：${data.status}`:'结构化 Finding 已写入');else if(event==='outcome_snapshot')progress('核心承诺已由 Finding 计算完成');else if(event==='supplemental_artifact_created'){state.supplementalArtifacts=[data.artifact];progress('探索产物已按 supplemental 持久化');}else if(event==='final_block_delta'){renderBlocks([...state.blocks,data.block]);}else if(event==='turn_completed'){setStatus('分析完成');progress('最终答案已持久化');}else if(event==='turn_failed')throw new Error(data.message||data.error_code||'分析失败');}
    async function consumeSse(response){if(!response.ok){const body=await response.json().catch(()=>({}));throw new Error(body.error||'无法建立探索分析事件流。');}if(!response.body)throw new Error('事件流不可用。');const reader=response.body.getReader();const decoder=new TextDecoder();let buffer='';while(true){const{value,done}=await reader.read();if(done)break;buffer+=decoder.decode(value,{stream:true});const frames=buffer.split('\n\n');buffer=frames.pop()||'';for(const frame of frames){let event='';let data=null;for(const line of frame.split('\n')){if(line.startsWith('event: '))event=line.slice(7).trim();if(line.startsWith('data: '))data=JSON.parse(line.slice(6));}if(event&&data)handleEvent(event,data);}}}
    async function run(){if(state.running)return;state.running=true;byId('run').disabled=true;showError('');renderBlocks([]);state.supplementalArtifacts=[];byId('progress-list').innerHTML='';byId('progress').classList.remove('visible');try{if(!state.filename)await uploadSelected();setStatus('分析进行中');const response=await fetch('/api/v2/exploratory-python',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({filename:state.filename,metric:byId('metric').value.trim(),question:byId('question').value.trim(),purpose:byId('purpose').value.trim(),code:byId('code').value})});await consumeSse(response);}catch(error){showError(error.message);setStatus('分析失败');}finally{state.running=false;byId('run').disabled=false;}}
    async function restore(){const params=new URLSearchParams(window.location.search);state.sessionId||=params.get('session_id')||'';state.turnId||=params.get('turn_id')||'';if(!state.sessionId||!state.turnId){showError('当前页面没有可恢复的探索分析。');return;}showError('');setStatus('正在恢复');const response=await fetch(`/api/v2/sessions/${encodeURIComponent(state.sessionId)}/turns/${encodeURIComponent(state.turnId)}`);const body=await response.json();if(!response.ok){showError(body.error||'恢复失败');setStatus('恢复失败');return;}const c=body.request_context||{};if(c.filename){state.filename=c.filename;byId('file-state').textContent=`已关联：${c.filename}`;}if(c.metric)byId('metric').value=c.metric;if(c.question)byId('question').value=c.question;if(c.purpose)byId('purpose').value=c.purpose;state.supplementalArtifacts=body.supplemental_artifacts||[];renderBlocks(body.blocks||[]);updateUrl();setStatus('已从持久化消息块与补充产物恢复');}
    byId('run').addEventListener('click',run);byId('restore').addEventListener('click',restore);byId('file').addEventListener('change',()=>{state.filename='';byId('file-state').textContent='等待上传';});if(new URLSearchParams(window.location.search).has('session_id'))restore();
})();
