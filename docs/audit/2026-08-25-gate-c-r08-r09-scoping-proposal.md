# Gate C R08/R09 取舍建议书（未执行 Provider）

日期：2026-08-25。本文件是供用户决策的提案，不是任何批次的授权或通过声明。

## R08（无数据/追问）：建议真实调用 0 次

- R08 的验收面（两层意图、无数据澄清文案、追问绑定已有结果、无死路）已在 Slice 5 以 provider-neutral 与 Web 证据通过（见 [开放分析完整性冻结收据](2026-08-25-slice-5-open-analysis-integrity-freeze.md)）。
- R08 的故障模式（F06 文案、F26/F27 状态绑定）是确定性代码行为，不是模型判断行为；真实调用对它们没有区分力。模型判断维度（有界回答纪律）已由主模型 7/7 批次覆盖。
- 结论：不为 R08 消耗真实调用；若未来发现模型在歧义输入上的意图分类需要真实验证，用旅程级执行器在主模型上补 1 条，不单独立批。

## R09（高级能力可达性）：建议 1 条真实旅程，且排在 R07 旅程试点之后

- R09 的靶风险是"工具存在但实际不可达"——这是**模型路由行为**，离线回放脚本指定了工具选择，无法证明真实模型会选到高级工具；单次调用事实包批次也不含工具面，同样证明不了路由。
- 已建成的旅程级可数执行器正是为该风险设计的仪器：真实模型 + 真实工具面（64 schema）+ 轮次与预算硬上限。
- 建议：选 R01 旅程化作 R09 唯一真实调用试点（留存曲线问题 → 模型须路由到 `curve_fitting` 或给出事实理由），框架与 R07 试点一致（round_cap × 3 档阶梯）。适用才调用、不适用给事实理由的契约同时进判定。
- 排序理由：先用 R07（业务判断型旅程）验证执行器在真实 Provider 下的行为，再上 R09（路由型旅程）。

## 异构模型（kimi-k3 @ api.moonshot.cn）：单次调用批次形态，待主模型旅程试点通过后冻结

- 形态建议：复用 R01/R02/R04/R07 四场景事实包与 prompt（逐字节一致），仅换模型与端点——同 prompt 跨模型对比的是方法/范围/发布语义纪律，符合计划 §12 对异构批次的定义。
- 实施缺口（待做）：单次调用执行器目前把 `model_id` 与 `.env` 主配置绑定；需扩展为 manifest 声明 `api_base_env`/`api_key_env`（与旅程执行器同语义），凭据经环境变量注入。
- 用户动作：在 `.env` 增加 `MOONSHOT_API_KEY=...`（勿在对话中粘贴密钥）；执行仍需精确授权。

## 建议的 Gate C 收尾顺序

1. R07 旅程试点（真实调用，至多 18 次）——验证旅程执行器；
2. R09 路由旅程（真实调用，至多 18 次）——高级能力可达性；
3. 异构 kimi-k3 单次调用批次（4 场景 × 阶梯，至多 12 次）——跨模型纪律；
4. R08 零调用闭环（以 Slice 5 收据为准）；
5. 汇总 Gate C 收据链，进入 Gate D 审阅。

每步仍单独冻结、单独授权；失败即停。
